import { Routes } from '@angular/router';
import { BoardingsComponent } from './components/boardings/boardings.component';

export const routes: Routes = [
  { path: 'boardings', component: BoardingsComponent },
];
